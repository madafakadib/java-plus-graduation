server:
  port: 8080

spring:
  application:
    name: gateway-server

  config:
    import: optional:configserver:http://config-server

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/